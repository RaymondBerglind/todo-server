const assert = require('assert');

describe('REMOVEME: Dummy test suite', function () {
    describe('REMOVEME: Dummy test', function () {
        it('Should be removed, but should also succeed.', function () {
            assert.equal(true, true);
        });
    });
});